[package]
name = "undo"
version = "0.2.0"
authors = ["evenorog <evenorog@gmail.com>"]
description = "A undo/redo library based on the command pattern."
documentation = "https://docs.rs/undo"
repository = "https://github.com/evenorog/undo"
readme = "README.md"
license = "MIT"
keywords = ["undo", "redo"]

[dependencies]
fnv = "1.0.5"
